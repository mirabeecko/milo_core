{
  "buildCommand": "pnpm install --no-frozen-lockfile && pnpm --filter @milo/web build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": "nextjs"
}
